#include <iostream>
using namespace std;

int main{int argc,char* argv[]} {
    // command line options
    bool testMode = false;
    if (int i = 1; i < argc; i++) {
        if (argv[i] == "-test") {
            tsetMode = true;
        } else if (argv[] == "-load" && argv[++i] == "file") {
            // load file operations
        }
    }

    // read commands
}